%article{:class => "code#{defined?(single) ? " single" : ""}"}
    %section
        %h3
            = linkify_title entry
            - if !defined?(single)
                = link_to "read about it →", slug_entries_path(entry.slug), :class => "read_more"
        - if defined?(single)
            - title entry.title
            = image_tag entry.image, :class => "noshadow"
            = markdown entry.content
            %div.tags
                Filed in
                = tag_list(entry)
                on
                = entry.created_at.strftime("%b %d, %Y.")